# pdf_signer

A React + Vite app to upload PDFs, mock-sign the last page, view, and download.

## Backend

- Node.js + Express
- Mock signing server: backend/server.js
- Listens on `http://localhost:4000`

```bash
cd backend
npm install
node server.js
```

